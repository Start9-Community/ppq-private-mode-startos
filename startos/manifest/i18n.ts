export const short = {
  en_US: 'End-to-end encrypted proxy for PPQ.AI private (TEE) AI models',
  es_ES:
    'Proxy cifrado de extremo a extremo para los modelos de IA privados (TEE) de PPQ.AI',
  de_DE:
    'Ende-zu-Ende-verschlüsselter Proxy für private (TEE) KI-Modelle von PPQ.AI',
  pl_PL: 'Szyfrowany end-to-end proxy dla prywatnych modeli AI (TEE) PPQ.AI',
  fr_FR:
    'Proxy chiffré de bout en bout pour les modèles IA privés (TEE) de PPQ.AI',
}

export const long = {
  en_US:
    'Run AI queries that nobody — not even PPQ.AI — can read. This proxy encrypts every request on your server before it leaves, and it is only decrypted inside a hardware-secured enclave (Trusted Execution Environment) that is cryptographically attested before any data is sent. Point any OpenAI- or Anthropic-compatible client (including Claude Code) at the local endpoint and pay per query with a PPQ.AI API key — no subscription required.',
  es_ES:
    'Ejecuta consultas de IA que nadie — ni siquiera PPQ.AI — puede leer. Este proxy cifra cada solicitud en tu servidor antes de que salga, y solo se descifra dentro de un enclave protegido por hardware (Trusted Execution Environment) que se verifica criptográficamente antes de enviar cualquier dato. Apunta cualquier cliente compatible con OpenAI o Anthropic (incluido Claude Code) al endpoint local y paga por consulta con una clave API de PPQ.AI — sin suscripción.',
  de_DE:
    'Führe KI-Anfragen aus, die niemand — nicht einmal PPQ.AI — lesen kann. Dieser Proxy verschlüsselt jede Anfrage auf deinem Server, bevor sie ihn verlässt; entschlüsselt wird sie nur innerhalb einer hardwaregesicherten Enklave (Trusted Execution Environment), die vor jeder Datenübertragung kryptografisch attestiert wird. Verbinde jeden OpenAI- oder Anthropic-kompatiblen Client (einschließlich Claude Code) mit dem lokalen Endpunkt und zahle pro Anfrage mit einem PPQ.AI-API-Schlüssel — ohne Abonnement.',
  pl_PL:
    'Wykonuj zapytania AI, których nikt — nawet PPQ.AI — nie może odczytać. Ten proxy szyfruje każde żądanie na Twoim serwerze, zanim je opuści, a odszyfrowanie następuje wyłącznie wewnątrz sprzętowo zabezpieczonej enklawy (Trusted Execution Environment), kryptograficznie weryfikowanej przed wysłaniem jakichkolwiek danych. Skieruj dowolnego klienta zgodnego z OpenAI lub Anthropic (w tym Claude Code) na lokalny endpoint i płać za zapytanie kluczem API PPQ.AI — bez subskrypcji.',
  fr_FR:
    "Exécutez des requêtes IA que personne — pas même PPQ.AI — ne peut lire. Ce proxy chiffre chaque requête sur votre serveur avant qu'elle ne parte, et elle n'est déchiffrée qu'à l'intérieur d'une enclave sécurisée par le matériel (Trusted Execution Environment), attestée cryptographiquement avant tout envoi de données. Pointez n'importe quel client compatible OpenAI ou Anthropic (y compris Claude Code) vers le point de terminaison local et payez à la requête avec une clé API PPQ.AI — sans abonnement.",
}
